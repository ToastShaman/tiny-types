dependencies {
    api project(':core')

    implementation(platform(Libs.jackson_bom))
    implementation(Libs.jackson_databind)
    implementation(Libs.jackson_module_parameter_names)

    testImplementation project(path: ":core", configuration: 'testArtifacts')
}
